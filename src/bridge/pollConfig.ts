// ── Bridge stub — remote-control feature removed ────

export function getPollIntervalConfig(): { pollIntervalMs: number; maxPollIntervalMs: number } {
  return { pollIntervalMs: 5000, maxPollIntervalMs: 30000 }
}
