// ── Memory age stub — replaced by dual-agent graph memory system ────

export function memoryAgeDays(_mtimeMs: number): number {
  return 0
}

export function memoryAge(_mtimeMs: number): string {
  return ''
}

export function memoryFreshnessText(_mtimeMs: number): string {
  return ''
}

export function memoryFreshnessNote(_mtimeMs: number): string {
  return ''
}
